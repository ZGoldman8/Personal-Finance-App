<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.personalfinanceapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PersonalFinanceApp">
        <activity android:name=".NetWorthCalculatorActivity" android:label="@string/net_worth_calculator" android:parentActivityName=".MainActivity"/>
        <activity android:name=".NetWorthProgressionActivity" android:label="@string/net_worth_progression" android:parentActivityName=".MainActivity"/>
        <activity android:name=".AssetsInventoryActivity" android:label="@string/assets_inventory" android:parentActivityName=".MainActivity" android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".RebalancePortfolioActivity" android:label="@string/rebalance_portfolio" android:parentActivityName=".MainActivity"/>
        <activity android:name=".InvestmentPerformanceActivity" android:label="@string/investment_performance" android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.PersonalFinanceApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>